{
  "name": "dynamic_rest_query",
  "version": "1.0.4",
  "description": "Allows the use of MongoDB agregation operators in REST queries, without dependencies forcing you into a schema.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "test": "node dist/test.js",
    "prepublish": "npm run build"
  },
  "keywords": [
    "nosql",
    "mongo",
    "rest",
    "query",
    "get",
    "http",
    "request"
  ],
  "author": "Juan Antonio Villagomez",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "deepmerge": "^2.1.1"
  }
}
